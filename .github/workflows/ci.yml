on:
  push:
    branches:
      - develop
  workflow_dispatch:

permissions:
  id-token: write
  contents: write

jobs:
  pipeline:
    uses: theSamsCo/central-workflows/.github/workflows/pipeline.yaml@v1
    with:
      env: ${{ github.ref == 'refs/heads/main' && 'prod' || 'dev' }}
      app_name: teste
      gcp_wif_provider: ${{ vars.GCP_WIF_PROVIDER_DEV }}
      gcp_sa: ${{ vars.GCP_SERVICE_ACCOUNT_DEV }}
    secrets: inherit
